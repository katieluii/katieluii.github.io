# Dependencies
node_modules/

# Build output (CI rebuilds from source)
dist/

# Secrets / local env
.env
.env.*

# OS / editor cruft
.DS_Store
**/.DS_Store

# Alternative lockfile (CI uses npm + package-lock.json)
bun.lock
__pycache__/

# transactional sync staging dir (never committed)
src/data/.atlas-staging/
src/data/atlas/.*.bak-*

# Dated .xlsx exports from scripts/2026-Stock_conc.py (local analysis artifact)
*_stock_conc.xlsx

# advisory-lock sidecars from atomic writers — never content
*.json.lock

# Wrapper logs capture the sync abort banner, which QUOTES the internal
# lines the leak gate rejected. This repo is public — never commit them.
logs/
